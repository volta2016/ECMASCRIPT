const numbers = [1, 2, 3, 4, 5];
console.log(numbers.join()); //1, 2, 3, 4, 5]
console.log(numbers.join("-")); //1-2-3-4-5]

/*the join method creates an returns a new string by joining each element of the
given array sparated by a comma by dafault or by a specific separator character if
the user provides any*/
