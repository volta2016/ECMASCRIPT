const numbers = [1, 2, 3, 4, 5];
const results = numbers.find((element) => element > 2);

console.log(results); //3

/*the find method iterates over each element in the array, and it
returns the first element that satisfies the given condition*/
