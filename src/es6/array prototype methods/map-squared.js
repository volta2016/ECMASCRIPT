const numbers = [1, 2, 3, 4, 5];
const squared = numbers.map((e) => e ** 2);
console.log(squared);

/*The method map takes an individual element of the given array 
and changes its value according to the passed function, then returns a
new array with those modified values */
